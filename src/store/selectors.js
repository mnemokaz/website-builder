export const selectTotal = state => state.total;

export const selectHeader = state => state.header;

export const selectContent1 = state => state.content1;

export const selectContent2 = state => state.content2;

export const selectContent3 = state => state.content3;

export const selectFooter = state => state.footer;
